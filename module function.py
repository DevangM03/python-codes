import math
# shows all the functions and constants available in the math module
print(dir(math))
# dir() is useful to see everything available in a module
from math import * # imports all functions and constants from math directly
print(sqrt(16)) 